# Dataset Folder

Put the Oxford-IIIT Pet dataset here before running the notebook.

Expected layout:

```text
data/
  oxford_pet/
    oxford_pet/
      images/
      annotations/
        trimaps/
        trainval.txt
        test.txt
```

The dataset files are intentionally ignored by Git because they are large and can be downloaded separately.
